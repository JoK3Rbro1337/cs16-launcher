export default function Home(): React.JSX.Element {
  return (
    <section className="page">
      <h1>Play</h1>
      <p className="muted">
        M0 scaffold. The Play button and Steam detection arrive in M1–M2.
      </p>
      <button className="primary" disabled>
        Play (M2)
      </button>
    </section>
  )
}
