export default function Servers(): React.JSX.Element {
  return (
    <section className="page">
      <h1>Servers</h1>
      <p className="muted">Server browser (master-server + A2S query) arrives in M4.</p>
    </section>
  )
}
